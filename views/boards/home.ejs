<!DOCTYPE html>
<html lang="jp">
<head>
    <meta http-equiv="content-type"
          content="text/html; charset=UTF8">
    <title><%= title %></title>
    <link rel="stylesheet" 
          href="http://stackpath.bootstrap/4.4.1/css/
                bootstrap.min.css"
          crossorigin="anonymous">
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<body>
    <header>
          <h1><%= title %></h1>
    </header>
    <div role="main">
          <p><%= userName %>'s message.</p>
          <table>
                <% for(let i in content) { %>
                  <%- include('data_item', {val:content[i]}) %>
            <% } %>
          </table>

          <ul class="pagintion justify-content-center">
            <li>
                <a href="/boards/home/<%=userName %>/<%=userId%>/
                  <%= page - 1 %>"
                  class="page-link">&lt;&lt; Prev</a>
            </li>

            <li>
                  <a href="/boards/home/<%=userName %>/<%=userId%>/
                        <%= page + 1 %>"
                    class="page-link">Next &gt;&gt;</a>
            </li>
        </ul>
    </div>
    <div class="text-left">
          <a href="/boards">&lt;&lt; Top.</a>
    </div>
</body>
</html>